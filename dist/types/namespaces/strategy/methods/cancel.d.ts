/**
 * strategy.cancel() - Cancel a pending order by its ID
 *
 * @param id - A required parameter. The order identifier to cancel.
 */
export declare function cancel(context: any): (id: string) => any;
