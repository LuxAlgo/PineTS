/**
 * strategy.param() - Internal method for handling parameters
 * Used by the transpiler to wrap constant access
 */
export declare function param(context: any): (source: any, index: any, name?: string) => any;
